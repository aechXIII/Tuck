{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main Tuck window to use only the typed backend request command.",
  "windows": ["main"],
  "permissions": ["allow-backend-request"]
}
